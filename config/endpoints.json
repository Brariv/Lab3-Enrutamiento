{
  "_comment": "Mapea cada endpoint (ATM/servidor, NO son routers) a su router gateway. No modifica config/topologia.json (el grafo compartido con las otras parejas); solo dice por cual router de ESE grafo se cuelga cada endpoint local.",
  "Brandon": "W",
  "Promerica": "W"
}
